test:
  imports:
    - sync_pre_commit_hooks
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/wpk-nist-gov/sync-pre-commit-hooks
  summary: Some useful pre-commit hooks dependencies, etc.
  description: |
    Some useful pre-commit hooks dependencies, etc.
  license: NIST-PD
  license_file: LICENSE

extra:
  recipe-maintainers:
    - wpk-nist-gov
